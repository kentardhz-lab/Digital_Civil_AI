project:
  name: demo_project

run:
  # optional; اگر خالی باشد خودکار timestamp می‌سازد
  id: ""

inputs:
  # فعلاً CSV؛ بعداً Excel/IFC اضافه می‌کنیم
  elements_csv: data/elements.csv

scenarios:
  base: true
  degraded: true
  extreme: true

output:
  root_dir: outputs
  keep_config_copy: true
  write_pip_freeze: true
